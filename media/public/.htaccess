Options -MultiViews
# Root-level .htaccess intentionally keeps no global rewrite rules
# so that subdirectories like /media/ and /jinzai/ control routing
# via their own .htaccess files.

# Optional: cache headers can be added here if needed, but avoid
# overriding sub-app .htaccess behavior.
